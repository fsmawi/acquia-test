import { ContextLinkDirective } from './context-link.directive';

describe('ContextLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new ContextLinkDirective();
    expect(directive).toBeTruthy();
  });
});
