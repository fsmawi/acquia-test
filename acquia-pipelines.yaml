version: 1.2.0

events:
  build:
    steps:
      - install:
          script:
            - 'nvm install v8.11.3'
            - 'nvm use 8.11.3'
            - 'npm install'
      - test:
          script:
            - node $SOURCE_DIR/scripts/cpu-load-generator.js
