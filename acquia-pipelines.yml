version: 1.0.0

events:
  build:
    steps:
      - hello:
          type: script
          script:
            - echo "test commit!"
            - echo "test commit! xxx"
            - cat /etc/hosts
            - echo "127.0.0.1 example.acquia.com example" >> /etc/hosts
            - cat /etc/hosts
