version: 1.0.0

events:
  build:
    steps:
      - hello:
          type: script
          script:
            - ls -l ~/.ssh
            - cat ~/.ssh/config
            - cd $BUILD_DIR && git ls-remote
            - echo "TEST DEPLOY 33333"
  post-deploy:
    steps:
      - e2e_test:
          script:
            - ls -l ~/.ssh
            - cat ~/.ssh/config
            - cd $BUILD_DIR && git ls-remote
            - echo "TEST DEPLOY 44444"
