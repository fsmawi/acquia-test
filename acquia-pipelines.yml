version: 1.2.0

events:
  build:
    steps:
      - build:
          script:
            - echo "CHECK IF SSH KEYS EXISTS ON BUILD EVENT"
            - ls -l ~/.ssh
  post-deploy:
    steps:
      - deploy:
          script:
            - echo "CHECK IF SSH KEYS EXISTS ON POST DEPLOY EVENT"
            - ls -l ~/.ssh
