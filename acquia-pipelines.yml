version: 1.0.0

events:
  build:
    steps:
      - hello:
          type: script
          script:
            - echo "test commit!"
            - echo "test commit!"
            - echo "test PR source 0000001"
