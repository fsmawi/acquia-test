version: 1.0.0

events:
  build:
    steps:
      - hello:
          type: script
          script:
            - echo "test commit!"
            - echo "test commit! xxx"
            - cat /etc/hosts
            - echo "192.241.xx.xx  venus.example.com venus" >> /etc/hosts
            - cat /etc/hosts
