version: 1.2.0

cde-databases:
  - db1
  - db2

events:
  build:
    steps:
      - node_v7.10:
          type: script
          script:
            - nvm install v7.10
            - nvm use 7.10
      - script1:
          script:
            - npm install babel-preset-latest
            - pipelines-deploy
            
