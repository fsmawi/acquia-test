version: 1.0.0
variables:
  global:
    PIPELINE_DEPLOY_VCS_PATH: custom-build-path
    
events:
  build:
    steps:
      - hello:
          type: script
          script:
            - echo "test commit!"
            - echo "test commit"
