version: 1.0.0
variables:
  global:
    ARRAY_VAR: 
      - item_1
      - item_2
      - item_3
      
events:
  build:
    steps:
      - hello:
          type: script
          script:
            - php -r 'print_r(getenv("ARRAY_VAR"));'
